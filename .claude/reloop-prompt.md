Fix 7 review issues then migrate entire codebase from JS to TypeScript. Phase 1: add missing tests for quick-start/my-work/slugify/buildCache, replace cp -Rc with fs.cpSync in cache.js, update README for v0.2.0, fix stale config in reset handler, separate cache hash files per setupCwd. Phase 2: add tsconfig.json with strict ESM, rename all lib and lib/engine .js to .ts with type annotations, rename test files to .ts, update package.json scripts to use tsx, verify all 92 tests pass after each file. Skip dashboard JS files.
